[package]
name = "guiltty-sprite"
version = "0.0.0"
publish = false
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Movable sprites (Bitmap/Sprite) drawn onto a guiltty-core Canvas"

[lints]
workspace = true

[dependencies]
guiltty-core = { path = "../guiltty-core", version = "0.0.0" }
image = { version = "0.25.10", default-features = false, features = ["png", "jpeg", "gif", "bmp"] }
