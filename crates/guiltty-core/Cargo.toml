[package]
name = "guiltty-core"
version = "0.0.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Backend-agnostic canvas, shapes, text, sprites, regions, and zoom/scroll logic for guiltty"

[dependencies]
