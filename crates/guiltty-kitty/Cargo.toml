[package]
name = "guiltty-kitty"
version = "0.0.0"
publish = false
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Kitty graphics protocol backend for guiltty (pure Rust, no C FFI)"

[lints]
workspace = true

[dependencies]
guiltty-core = { path = "../guiltty-core" }
