//! Placeholder example. Real demos land alongside the corresponding guiltty-core features;
//! run with `cargo run -p guiltty-examples --bin placeholder`.

fn main() {
    println!("guiltty-examples: no demos yet — the scaffold API has no drawable features");
}
